const height=10;
const base=4;
const permiter=2*(height+base);
const area=height*base;

console.log(`the perimeter of rectangule with ${height} and ${base} and its side length is ${perimeter}`);
console.log(`the area of rectangule with ${height} and ${base} and its side length is ${area}`);